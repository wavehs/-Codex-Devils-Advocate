interface:
  display_name: "Adversarial Review"
  short_description: "Fail-closed independent correctness review"
  default_prompt: "Run $adversarial-review on the completed code change and return PASS, FAIL, or INCONCLUSIVE."
policy:
  allow_implicit_invocation: false
