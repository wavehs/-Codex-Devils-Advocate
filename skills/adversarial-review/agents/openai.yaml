interface:
  display_name: "Adversarial Review"
  short_description: "Fail-closed review with runtime preflight"
  default_prompt: "Run $adversarial-review in STRICT mode on the completed code change and return PASS, FAIL, or INCONCLUSIVE."
policy:
  allow_implicit_invocation: false
